[] Only output cost, rather than usage and cost. Set `unit=Unit.USD` when calling vue.get_chart_usage to get back dollars directly. Remove other rate calculations and kwh output data.
[] Output config logic is incorrect. Current behavior is one column per device if there is no config at all. Desired behavior is one column per device for any device not mentioned in the output config. It is ok to assume that each output column will either be all channels for a particular device, or a subset of channels for a single device. Multiple devices will not be combined into one output channel.
